@extends('layouts.public')
@section('content')
<main class="max-w-4xl mx-auto px-4 py-10"><h1 class="text-3xl font-bold">Nani Dadi Ke Nuskhe — Import</h1><p class="mt-2 text-slate-600">Upload CSV or XLSX. Invalid rows are reported without stopping valid rows.</p><a class="inline-block mt-4 text-teal-700 underline" href="{{ route('admin.nani-dadi.template') }}">Download template</a>
@if(session('import_result')) @php($r=session('import_result'))<div class="mt-6 rounded-xl bg-slate-100 p-4">Total: {{$r['total']}} · Created: {{$r['created']}} · Updated: {{$r['updated']}} · Failed: {{$r['failed']}} @if($r['errors'])<ul class="mt-3 list-disc pl-5 text-red-700">@foreach($r['errors'] as $line=>$errors)<li>Row {{$line}}: {{implode('; ',$errors)}}</li>@endforeach</ul>@endif</div>@endif
<form method="post" enctype="multipart/form-data" action="{{route('admin.nani-dadi.import')}}" class="mt-8 space-y-5 rounded-2xl bg-white p-6 shadow">@csrf<label class="block">File<input class="mt-2 block w-full" type="file" name="file" required accept=".csv,.xlsx"></label><label class="block">Mode<select name="mode" class="mt-2 rounded border p-2"><option value="create_update">Create + Update</option><option value="create_only">Create only</option><option value="update_only">Update only</option></select></label><button class="rounded bg-teal-700 px-5 py-2 text-white">Import</button></form></main>
@endsection
