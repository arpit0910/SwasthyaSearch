@extends('layouts.public') @section('content')<main class="mx-auto max-w-7xl px-4 py-10"><div class="flex items-center justify-between"><h1 class="text-3xl font-bold">Nani Dadi Ke Nuskhe</h1><div><a class="rounded bg-teal-700 px-4 py-2 text-white" href="{{route('admin.nani-dadi.create')}}">Add Nuskha</a> <a class="rounded border px-4 py-2" href="{{route('admin.nani-dadi.import.form')}}">Import</a></div></div>@if(session('success'))<p class="mt-4 rounded bg-green-50 p-3 text-green-800">{{session('success')}}</p>@endif<form class="mt-6"><input name="q" value="{{request('q')}}" class="rounded border p-2" placeholder="Search"><button class="rounded bg-slate-800 px-4 py-2 text-white">Filter</button></form><div class="mt-6 overflow-auto rounded-xl bg-white shadow"><table class="w-full text-left"><thead><tr class="border-b"><th class="p-3">ID</th><th class="p-3">Title / Hindi</th><th class="p-3">Category</th><th class="p-3">Evidence</th><th class="p-3">Status</th><th class="p-3">Actions</th></tr></thead><tbody>@foreach($items as $x)<tr class="border-b"><td class="p-3">{{$x->id}}</td><td class="p-3">{{e($x->title_en)}}<br><span class="text-slate-500">{{e($x->title_hi)}}</span></td><td class="p-3">{{e($x->category?->name_en)}}</td><td class="p-3">{{$x->evidence_label}}</td><td class="p-3">{{$x->is_published?'Published':'Draft'}}</td><td class="p-3"><a class="text-teal-700" href="{{route('admin.nani-dadi.edit',$x)}}">Edit</a> <form class="inline" method="post" action="{{route('admin.nani-dadi.toggle',$x)}}">@csrf<button class="ml-2 text-amber-700">{{$x->is_published?'Unpublish':'Publish'}}</button></form><form class="inline" method="post" action="{{route('admin.nani-dadi.destroy',$x)}}">@csrf @method('delete')<button class="ml-2 text-red-700">Delete</button></form></td></tr>@endforeach</tbody></table></div><div class="mt-5">{{$items->links()}}</div></main>@endsection
